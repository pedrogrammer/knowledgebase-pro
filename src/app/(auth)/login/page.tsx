import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Login | KnowledgeBase Pro",
  description: "Login to access the knowledge base",
};

const LoginPage = () => {
  return <div>LoginPage</div>;
};

export default LoginPage;
